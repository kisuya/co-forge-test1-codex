{
  "project": "{{PROJECT_ID}}",
  "goal": "{{GOAL}}",
  "agent": "",
  "features": [
    {
      "id": "[domain]-[NNN]",
      "name": "[Feature name]",
      "description": "[Mini-spec: 1)inputs/outputs 2)success behavior 3)error/edge cases 4)UX feedback 5)boundary conditions]",
      "status": "pending",
      "priority": 1,
      "test_file": "tests/[path]/test_[name].[ext]",
      "depends_on": []
    }
  ]
}

// Feature decomposition rules:
// - Each feature: 30 min - 2 hours of work
// - Unique ID: [domain]-NNN (continue numbering from previous projects)
// - Maps to 1-2 acceptance criteria from PRD
// - description: agent-ready mini-spec (no guessing needed)
//     Include: 1) inputs/outputs  2) success behavior  3) error/edge cases
//              4) UX feedback     5) boundary conditions
// - test_file: specific path, even if test doesn't exist yet
// - depends_on: feature IDs that must be "done" first
// - priority: 1 = highest. No ties — strict ordering.
